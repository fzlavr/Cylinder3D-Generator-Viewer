// ─── CREATED: extracted from test.jsx ────────────────────────
import { CM_TO_PX } from "./constants.js";

export function cmToPx(cm) { return cm * CM_TO_PX; }
export function pxToCm(px) { return px / CM_TO_PX; }
